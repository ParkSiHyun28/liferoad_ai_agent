# Streamlit Community Cloud 배포용 secrets 견본.
# 앱 설정 화면의 Secrets 탭에 아래 내용을 실제 값으로 채워 붙여넣는다.
# 로컬 개발은 이 파일이 아니라 .env를 쓴다.

LLM_PROVIDER = "gemini"
GEMINI_API_KEY = "AIza_여기에_실제_키"
GEMINI_MODEL = "gemini-2.5-flash"

# 본선에서 Claude로 전환하려면 위 LLM_PROVIDER를 "claude"로 바꾸고 아래 키를 채운다.
# ANTHROPIC_API_KEY = "sk-ant-..."
# CLAUDE_MODEL = "claude-opus-4-8"
